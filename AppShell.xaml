<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="course4Hotel.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:course4Hotel.View"
    Title="course4Hotel">

    <TabBar>
        <ShellContent
            Title="Повідомлення"
            ContentTemplate="{DataTemplate view:NotificationsAdmin}"
            
            />
        <ShellContent
            Title="Номери"
            ContentTemplate="{DataTemplate view:AdminRooms}"
            
            />
        <ShellContent
            Title="Сервіси"
            ContentTemplate="{DataTemplate view:AdminServices}"
           
    />
    </TabBar>

</Shell>
